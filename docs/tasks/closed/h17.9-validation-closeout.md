# H17.9 Validation and Closeout Evidence (2026-03-06)
Status: historical archived reference; not authoritative for current work.

## Scope
Closeout evidence for H17 canonical Scopus migration slices H17.1-H17.8.

## Required Gates

1. `./gradlew compileJava`
   - Result: `BUILD SUCCESSFUL`
2. `./gradlew test --tests '*ScopusImportEventIngestionServiceTest' --tests '*ScopusFactBuilderServiceTest' --tests '*ScopusProjectionBuilderServiceTest' --tests '*ScopusCanonicalIndexMaintenanceServiceTest' --tests '*ScopusBigBangMigrationServiceTest' --tests '*ScopusUpdateSchedulerTest' --tests '*ScopusCutoverGuardrailTest' --tests '*ScopusForumQueryServiceTest' --tests '*ScopusAuthorQueryServiceTest' --tests '*ScopusAffiliationQueryServiceTest'`
   - Result: `BUILD SUCCESSFUL`
3. `./gradlew check`
   - Result: `BUILD SUCCESSFUL`

## Replay and Idempotence Evidence

1. Import-event idempotence
   - `ScopusImportEventIngestionServiceTest#ingestSkipsWhenInsertHitsDuplicateKey`
   - Evidence: duplicate-key ingest is skipped (no duplicate event creation), preserving idempotent event-ledger behavior.
2. Fact-layer replay safety
   - `ScopusFactBuilderServiceTest#buildFactsFromImportEventsIsReplaySafeWithExistingFacts`
   - Evidence: replay updates existing facts deterministically without duplicate identity creation.
3. Projection enrichment replay safety
   - `ScopusProjectionBuilderServiceTest#rebuildViewsPreservesExistingWosEnrichment`
   - Evidence: projection rebuild preserves non-Scopus enrichment ownership fields (`wosId`) across rebuilds.

## Regression Guardrail Evidence

1. Runtime/read-cutover guardrails
   - `ScopusCutoverGuardrailTest#projectionBackedReadFacadesDoNotCallLegacyScopusRepositories`
2. Scheduler canonicalization guardrails
   - `ScopusCutoverGuardrailTest#schedulerTaskFlowUsesCanonicalPipelineHooks`
3. Ingestion entrypoint guardrails
   - `ScopusCutoverGuardrailTest#canonicalScopusDataServiceEntrypointsDoNotDirectlyWriteLegacyEntities`

These guardrails fail fast if legacy direct Scopus repository patterns are reintroduced in migrated runtime/scheduler/ingestion paths.

## Closeout

H17.9 validation evidence is complete for local gates and targeted Scopus regression coverage.
