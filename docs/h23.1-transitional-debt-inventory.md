# H23.1 Transitional Debt Inventory and Classification

Date: 2026-03-13

Scope: internal legacy/transitional flow retirement and simplification for H23.

## Classification Rules

- `remove now`: dead or misleading debt not required for runtime, parity gates, or public contract preservation.
- `keep intentionally`: still required for an explicit contract or operational tool.
- `defer`: real debt, but removal belongs to H23.2, H23.3, or a later dedicated slice because of public contract preservation or higher-risk wiring.

Owner defaults used in this inventory:

- Platform Backend Owner: runtime routing, redirects, internal naming debt, task follow-through.
- Frontend Owner: template/script allowances and frontend guardrails.
- Reporting Domain Owner: reporting lookup debt affecting CNFIS/WoS lookup flows.

## 1. Dual-Read-Only Mongo Adapters vs Runtime Adapters

| hotspot | current location(s) | runtime role | classification | owner | planned action | evidence |
|---|---|---|---|---|---|---|
| Mongo Scholardex parity read ports (`MongoScholardexAuthorReadPort`, `MongoScholardexForumReadPort`, `MongoScholardexAffiliationReadPort`, `MongoScholardexAdminReadPort`) | `src/main/java/ro/uvt/pokedex/core/service/application/MongoScholardex*ReadPort.java` | Non-runtime parity adapters for H22.6 gate; not used by Postgres-first query services | `keep intentionally` | Platform Backend Owner | Keep isolated for now, document them as dual-read-only, and treat deletion as an H23.2 follow-up only after the dual-read gate is retired | `JdbcDualReadGateService` injects all four Mongo ports for scenario comparison; runtime `Scholardex*QueryService` and `ScholardexAdminReadFacade` inject only Postgres ports and throw if absent |
| Mongo reporting lookup adapter (`ProjectionBackedReportingLookupFacade`) | `src/main/java/ro/uvt/pokedex/core/service/application/ProjectionBackedReportingLookupFacade.java` | Mongo-backed reporting lookup used by H22.6 parity scenarios, not by steady-state runtime `SwitchableReportingLookupFacade` | `keep intentionally` | Reporting Domain Owner | Keep as parity tooling while H22.6 remains active; isolate/document it as non-runtime debt and reevaluate in H23.2 when dual-read tooling scope is reduced | `JdbcDualReadGateService` injects `ProjectionBackedReportingLookupFacade` as `mongoReportingLookup`; `SwitchableReportingLookupFacade` resolves only `PostgresReportingLookupFacade` |
| Postgres-first query services still shaped like switchable routers (`ScholardexAuthorQueryService`, `ScholardexForumQueryService`, `ScholardexAffiliationQueryService`, `ScholardexAdminReadFacade`) | `src/main/java/ro/uvt/pokedex/core/service/application/Scholardex*QueryService.java`, `src/main/java/ro/uvt/pokedex/core/service/application/ScholardexAdminReadFacade.java` | Active runtime services; Postgres-only in practice, but still use `ObjectProvider` lookup plus missing-port guard | `defer` | Platform Backend Owner | Normalize to direct Postgres wiring and less transitional naming in H23.2/H23.3; no H23.1 behavior change | Each service obtains a Postgres port from `ObjectProvider`, throws if missing, and has no Mongo fallback branch |
| `SwitchableReportingLookupFacade` class name and indirection | `src/main/java/ro/uvt/pokedex/core/service/application/SwitchableReportingLookupFacade.java` | Active runtime reporting lookup entrypoint; Postgres-only in practice | `defer` | Platform Backend Owner | Rename/flatten in H23.2 so the class no longer suggests a runtime store toggle | The facade is `@Primary`, resolves only `PostgresReportingLookupFacade`, and throws if that bean is missing; `ScholardexCutoverGuardrailTest` asserts the old `app.reporting.read-store` runtime toggle must not exist |

## 2. Compatibility Redirects and Legacy Routes Still Used Internally

| hotspot | current location(s) | runtime role | classification | owner | planned action | evidence |
|---|---|---|---|---|---|---|
| Admin Scopus venue compatibility redirects (`/admin/scopus/venues`, `/admin/scopus/venues/edit/{id}`) | `src/main/java/ro/uvt/pokedex/core/view/AdminViewController.java` | Live compatibility redirects to the canonical `/admin/scholardex/forums` routes | `defer` | Platform Backend Owner | Keep in H23.1, document as compatibility debt, and decide in H23.2/H23.3 whether to remove redirects or keep them as explicit external compatibility shims | `showScopusVenuesPageCompatibilityRedirect()` returns `redirect:/admin/scholardex/forums`; `editScopusVenuePageCompatibilityRedirect()` returns `redirect:/admin/scholardex/forums/edit/{id}` |
| Mixed edit endpoint aliases for forum updates (`/scopus/forums/edit/{id}` and `/scopus/venues/edit/{id}`) | `src/main/java/ro/uvt/pokedex/core/view/AdminViewController.java` | Live write path accepts both canonical and compatibility route forms | `defer` | Platform Backend Owner | Keep behavior unchanged in H23.1; inventory for later narrowing once route compatibility requirements are explicitly decided | `@PostMapping({"/scopus/forums/edit/{id}", "/scopus/venues/edit/{id}"})` handles both forms |
| Legacy user CNFIS export route (`/user/export/cnfis`) | `src/main/java/ro/uvt/pokedex/core/view/UserViewController.java`, `docs/h03-reporting-contracts.md` | Live legacy export endpoint alongside newer `/user/publications/exportCNFIS2025` flow | `keep intentionally` | Reporting Domain Owner | Keep as an explicit contract-preservation route until CNFIS export deprecation is planned; note that cleanup is out of scope for H23.1 | `UserViewController#exportCnfisResults` still exposes `/user/export/cnfis`; `docs/h03-reporting-contracts.md` states “legacy route remains available and facade-backed” |
| User ranking redirect (`/user/rankings/{id} -> /scholardex/forums/{id}`) | `src/main/java/ro/uvt/pokedex/core/view/UserViewController.java` | Live compatibility redirect from user zone to canonical ranking page | `defer` | Platform Backend Owner | Keep in H23.1 and review in H23.2 only if route consolidation is worth the churn | `showRankingPage()` returns `redirect:/scholardex/forums/` + id |
| Admin Scopus route compatibility after template renames (`/admin/scopus/forums` and `/admin/scopus/venues*` mapped to renamed `admin/scholardex-*` templates) | `src/main/java/ro/uvt/pokedex/core/view/AdminViewController.java`, `src/main/resources/templates/admin/scholardex-forums.html`, `src/main/resources/templates/admin/scholardex-editForum.html` | Live compatibility route family retained after canonical Scholardex route/template cleanup | `defer` | Platform Backend Owner | Keep the redirect shims in H23.1 and revisit removal only when external/internal admin compatibility requirements are explicitly retired | `/admin/scopus/forums` redirects to `/admin/scholardex/forums`; canonical edit flow now renders `admin/scholardex-editForum` |

## 3. Legacy Scopus Naming on Steady-State Scholardex-Backed Paths

Public Scopus-branded contracts are listed separately from internal-only names so later cleanup does not break APIs by accident.

| hotspot | current location(s) | runtime role | classification | owner | planned action | evidence |
|---|---|---|---|---|---|---|
| Public API namespace `/api/scopus/**` backed by Scholardex query services | `src/main/java/ro/uvt/pokedex/core/controller/ScopusAuthorApiController.java`, `ScopusForumApiController.java`, `ScopusAffiliationApiController.java` | Public API contract; active and externally visible | `keep intentionally` | Platform Backend Owner | Preserve route shape in H23.x, but record it as public contract debt so internal cleanup does not rename endpoints | Each controller is `@RequestMapping("/api/scopus")` while delegating to `Scholardex*QueryService` |
| Public DTO names `Scopus*PageResponse` and `Scopus*ListItemResponse` on Scholardex-backed responses | `src/main/java/ro/uvt/pokedex/core/controller/dto/Scopus*Response.java`, `src/main/java/ro/uvt/pokedex/core/service/application/Scholardex*ReadPort.java` | Public response contract names used by active controllers/services | `keep intentionally` | Platform Backend Owner | Keep DTO names unchanged in H23.1; revisit only with explicit external contract migration planning | `ScholardexAuthorReadPort`, `ScholardexForumReadPort`, and `ScholardexAffiliationReadPort` all return `Scopus*PageResponse` types |
| Internal “Scopus” view-model and helper naming on steady-state admin reads (`AdminScopusPublicationSearchViewModel`, `AdminScopusCitationsViewModel`) | `src/main/java/ro/uvt/pokedex/core/service/application/model/AdminScopus*.java`, `src/main/java/ro/uvt/pokedex/core/service/application/ScholardexAdminReadFacade.java`, `MongoScholardexAdminReadPort.java`, `JdbcDualReadGateService.java` | Internal naming debt on active Scholardex/Postgres-backed admin paths | `defer` | Platform Backend Owner | Rename in later H23 follow-up work after public contract boundaries are clearly separated from internal model names | Admin read flow is named `ScholardexAdminReadFacade` but still exposes `AdminScopus*ViewModel` types |
| Internal runtime names that still imply runtime switching or legacy source authority (`SwitchableReportingLookupFacade`, `ProjectionBackedReportingLookupFacade`, `MongoScholardex*ReadPort`) | `src/main/java/ro/uvt/pokedex/core/service/application/*.java` | Internal-only naming; misleading now that runtime is Postgres-first and Mongo-backed ports are parity-only | `defer` | Platform Backend Owner | Rename/segregate in H23.2/H23.3 after the inventory is accepted | The active runtime lookup path is Postgres-first, while “switchable” and Mongo-prefixed classes remain in the same package and read as runtime-capable |
| Admin/user MVC route and task names that retain Scopus branding on Scholardex-backed behavior (`/admin/scopus/**`, `/user/publications/scopus_tasks`, internal `AdminScopus*` naming) | `src/main/java/ro/uvt/pokedex/core/view/AdminViewController.java`, `src/main/java/ro/uvt/pokedex/core/view/UserViewController.java`, `src/main/java/ro/uvt/pokedex/core/service/application/model/AdminScopus*.java` | Mixed public/internal surface naming, but still part of active UI contract | `defer` | Platform Backend Owner | Keep in place for H23.1; use the inventory to separate public UI contract debt from internal service renames | Active MVC compatibility routes and internal admin view models still use Scopus-branded names while canonical UI routes/templates are Scholardex-based |

## 4. Documented Transitional Frontend and Script Allowances Still Active

| hotspot | current location(s) | runtime role | classification | owner | planned action | evidence |
|---|---|---|---|---|---|---|
| `sb-admin-2.min.js` and legacy SB Admin styling | `src/main/resources/templates/fragments.html`, many `src/main/resources/templates/**` pages, `docs/h05-frontend-conventions.md` | Active global transitional frontend dependency | `keep intentionally` | Frontend Owner | Keep as the current allowed baseline; migrate only as dedicated frontend cleanup work removes dependency without broad UI churn | H05 explicitly says `sb-admin-2.min.js` is transitional and allowed short-term; `fragments.html` still includes `/js/sb-admin-2.min.js` and many templates include `/css/sb-admin-2.min.css` |
| `js/demo/datatables-demo.js` legacy demo script | Multiple admin/user/ranking templates under `src/main/resources/templates/**`, `docs/h05-frontend-conventions.md` | Active transitional dependency on many runtime pages | `keep intentionally` | Frontend Owner | Keep allowlisted for now, but treat spread reduction as future cleanup work rather than expanding usage | H05 explicitly allows `js/demo/datatables-demo.js`; many templates still include `/js/demo/datatables-demo.js` |
| H05-documented external asset allowlist (jQuery UI CSS for publication wizard, Chart.js for admin URAP details) | `docs/h05-frontend-conventions.md`, `scripts/verify-template-assets.js`, `src/main/resources/templates/user/publications-add-step1.html`, `user/publications-add-step2.html`, `admin/rankings-urap-details.html` | Active transitional external asset usage governed by guardrail allowlist | `keep intentionally` | Frontend Owner | Keep as the documented temporary allowlist until a dedicated migration slice removes them | H05 names these as transitional allowlist items; `verify-template-assets.js` allowlists the exact files and URLs |
| Active frontend guardrail allowlist drift beyond H05 doc (`frappe-charts` on admin rankings view) | `scripts/verify-template-assets.js`, `src/main/resources/templates/admin/rankings-view.html`, `src/main/resources/templates/rankings/wos-detail.html`, `docs/h05-frontend-conventions.md` | Transitional dependency still active, but not fully reflected in H05 doc text | `defer` | Frontend Owner | Reconcile H05 docs and guardrails in H23.5 or a frontend follow-up; decide whether to bless or retire `frappe-charts` usage | `verify-template-assets.js` allowlists `https://unpkg.com/frappe-charts@1.6.2/dist/frappe-charts.min.umd.js` for `admin/rankings-view.html`, while H05 only documents jQuery UI and Chart.js allowlists; `rankings/wos-detail.html` also still references the same CDN |
| Inline behavior scripts still active across admin/user templates | `scripts/verify-template-assets.js`, files such as `admin/citations.html`, `admin/scholardex-citations.html`, `user/citations.html`, `user/criteria-apply.html`, `user/profile.html`, `user/publications.html`, publication wizard pages | Transitional frontend debt with explicit current allowlist | `defer` | Frontend Owner | Keep inventory explicit and shrink the allowlist only when behavior is moved into `frontend/src/modules/**` | H05 says inline scripts are transitional debt only; `verify-template-assets.js` contains an explicit `allowlistedInlineScriptFiles` set for the still-active pages |

## 5. Inventory Conclusions for H23 Follow-On Work

- No ambiguous hotspots remain in the H23.1 scope. Every identified item is classified as `keep intentionally` or `defer`; no `remove now` candidates were confirmed from current runtime evidence.
- Dual-read Mongo adapters are not dead code today. They are operational H22.6 tooling and should stay isolated from runtime cleanup until the dual-read gate itself is retired.
- Public Scopus-branded routes and DTOs are active contract debt, not accidental leftovers. H23.1 should inventory them but not rename them.
- The biggest immediate cleanup opportunity for H23.2/H23.3 is internal naming and wiring simplification:
  - remove “switchable” semantics from Postgres-only runtime facades,
  - normalize admin route/template compatibility naming,
  - continue shrinking frontend inline-script and legacy-script allowances.
- `TASKS.md` does not require wording changes based on the current inventory. The task statement already matches the hotspots found in the repo, so this document can serve as the H23.1 source of truth.
