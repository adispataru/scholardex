package ro.uvt.pokedex.core.model.reporting;

public enum Position {
    ASIST_UNIV,
    LECT_UNIV,
    CONF_UNIV,
    PROF_UNIV,
    /** Abilitare (habilitation) — the 2026 standards define conf / prof / habilitation thresholds only
     *  (national assistant/lecturer standards were dropped). */
    HABIL,
    CS_I,
    CS_II,
    CS_III,
    ASIST_C,
    OTHER
}
